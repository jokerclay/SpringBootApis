# This file is used configure data source
# and other configuration properties
app.name= Employee Management Application
app.version = v1

# modify the server port
#server.port=6969

# Make the controller under /api/v1 globally
server.servlet.context-path=/api/v1
logging.level.org.springframework.web=DEBUG

# Config Date Source
spring.datasource.url=jdbc:mariadb://localhost:3306/mydb2
spring.datasource.username=root
spring.datasource.password=toor

# automatically create the database's tables and columns
spring.jpa.hibernate.ddl-auto=update

# Show the sql query that has been executed in the console
spring.jpa.show-sql=true




